# Variables de entorno para Hostinger
# Configurá estas en hPanel → Node.js → tu app → Environment variables

# Puerto HTTP (Hostinger asigna automáticamente, pero podés especificar)
PORT=4000

# Carpeta para la base de datos SQLite (debe ser escribible)
DB_DIR=storage

# Directorio del frontend compilado (opcional, por defecto: dist/)
# FRONTEND_DIR=dist

# Entorno de ejecución
NODE_ENV=production

# Orígenes permitidos para CORS (opcional, por defecto: todos)
# ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com

