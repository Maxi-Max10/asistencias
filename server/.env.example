# Ejemplo de variables de entorno para `server/`
# Copia este archivo como `.env` en la carpeta `server/` o configura estas
# variables en el gestor de Node.js de Hostinger.

# Puerto HTTP (Hostinger puede definir su propio puerto)
PORT=4000

# Carpeta donde se guardará la base de datos SQLite (debe ser escribible)
# Recomendado: una carpeta `storage` dentro del proyecto o un path absoluto en tu home
DB_DIR=../storage

# Directorio del build del frontend (opcional). Por defecto: ../web/dist
# FRONTEND_DIR=../web/dist

# Orígenes permitidos para CORS (opcional)
# ALLOWED_ORIGINS=https://midominio.com,https://admin.midominio.com
